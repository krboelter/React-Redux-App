import React from 'react';

export default function Selector() {
    return (
        <form>
            <select>
                <option className="op-1">Luke Skywalker</option>
                <option className="op-2">Han Solo</option>
            </select>
        </form>
    )
}